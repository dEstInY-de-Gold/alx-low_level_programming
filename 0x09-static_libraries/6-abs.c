#include "main.h"

/**
 * _abs - prints absolute value of a number
 * @n: int parameter to function
 *
 * Return: abolute value of input parameter
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
