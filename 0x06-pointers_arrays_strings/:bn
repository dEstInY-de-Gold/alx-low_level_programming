#include "main.h"

/**
 * reverse_array - reverse a string
 * @a: input string
 * @n: lengthof input string
 */

void reverse_array(int *a, int n)
{
	int tmp = b = 0;

	while ((n - 1) >= 0)
	{
		tmp = a[b];
		a[b] = a[n];
		a[n] = tmp;
		n--;
		b++;
	}
}
